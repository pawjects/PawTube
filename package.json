{
  "name": "pawtube",
  "version": "1.0.0",
  "description": "PawTube - A clean, distraction-free YouTube experience",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build",
    "start": "node server.js",
    "lint": "node -c server.js && node -c api/_piped.js && node -c api/health.js && node -c api/instances.js && node -c api/piped/trending.js && node -c api/piped/search.js && node -c api/piped/video.js && node -c api/piped/comments.js && node -c api/piped/channel.js && node -c api/piped/suggestions.js && node -c api/piped/streams.js && node -c api/piped/playlist.js && node -c api/piped/instances.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
