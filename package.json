{
  "name": "pawtube",
  "version": "1.0.0",
  "description": "PawTube - A clean, distraction-free YouTube experience",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "build": "echo 'No build required'",
    "start": "node server.js",
    "lint": "node -c docs/provider.js && node -c docs/api.js && node -c docs/app.js && node -c docs/player.js && node -c api/unified.js && node -c server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  }
}
